# Dependencies
node_modules/

# Environment variables (API keys — never upload this)
.env

# Uploaded audio temp files
uploads/

# Data directory (passwords, session secrets, API keys)
data/

# Prompt data (personal, stays local)
prompts.json

# OS files
.DS_Store
Thumbs.db
